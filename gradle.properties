# Project-wide Gradle settings (Bubblewrap reads this from the repo root).
# If the daemon fails with "Could not reserve enough space", lower -Xmx (e.g. 768m).
org.gradle.jvmargs=-Xmx1024m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.parallel=false
android.useAndroidX=true
